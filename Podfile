# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestAppInvite' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestAppInvite
	pod 'Firebase/Core'
    pod 'Firebase/Invites'
    pod 'Firebase/Auth'
    pod 'GoogleSignIn'

  target 'TestAppInviteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestAppInviteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
